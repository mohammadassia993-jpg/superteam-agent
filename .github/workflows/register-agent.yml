name: Get API Key
on: workflow_dispatch
jobs:
  get-key:
    runs-on: ubuntu-latest
    steps:
      - run: |
          RESPONSE=$(curl -s -X POST https://superteam.fun/api/agents -H "Content-Type: application/json" -d '{"name":"MyAgent"}')
          echo "$RESPONSE"
